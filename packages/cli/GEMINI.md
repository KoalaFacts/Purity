# @purity/cli — Gemini Context

```bash
npx @purity/cli my-app && cd my-app && npm install && npm run dev
```

Generates a ready-to-run Purity project with Vite, TypeScript, and AOT compilation pre-configured.
