# Vite / Vercel build artefacts
node_modules/
.vercel/
api/stream.js
api/chunks/

# Plugin-emitted manifest (ADRs 0032 + 0033). Per-machine absolute paths;
# regenerated by `npm run build` from `src/pages/`.
src/.purity/
