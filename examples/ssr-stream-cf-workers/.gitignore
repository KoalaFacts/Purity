# Wrangler / Vite output
node_modules/
dist/
.wrangler/

# Plugin-emitted manifest (ADRs 0032 + 0033). Regenerated by `npm run
# build` from `src/pages/`. Contains absolute paths and is per-machine.
src/.purity/
